/**
 * cosmos-providers/react — presentational payment UI kit.
 *
 * Client-safe: no network calls, no API keys. Components take plain typed
 * props; use `cosmos-providers/react/server` to turn SDK responses
 * (RampOrderData, Charge, Order, OrderReceipt...) into those props.
 */

export * from "./primitives";
export * from "./screens";
